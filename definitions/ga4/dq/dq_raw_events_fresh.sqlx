config { type: "assertion", schema: dataform.projectConfig.vars.schema, tags: ["ga4","dq","freshness"] }

SELECT 1
FROM ${ref("raw_events")}
WHERE event_ts < TIMESTAMP_SUB(CURRENT_TIMESTAMP(), INTERVAL 12 HOUR)
LIMIT 1

