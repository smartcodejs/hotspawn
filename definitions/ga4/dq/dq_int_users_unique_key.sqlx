config { type: "assertion", schema: dataform.projectConfig.vars.schema, tags: ["ga4","dq","users"] }

WITH d AS (
  SELECT user_pseudo_id, COUNT(*) c
  FROM ${ref("int_users")}
  GROUP BY 1
)
SELECT 1 FROM d WHERE c > 1 LIMIT 1
