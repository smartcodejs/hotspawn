warehouse: bigquery
defaultDatabase: hotspawn-440312
defaultSchema: dataform        # just a default; we override via vars
defaultLocation: EU            # IMPORTANT to avoid EU/US errors
vars:
  schema: ga4_staging          # overridden per release
  timezone: Europe/Bratislava
  backfill_days: 3



